import pyodbc

def get_conn():
    """
    Example database connection template.

    Actual server and database credentials are not included
    in the repository. Configure locally using environment
    variables or a private configuration file.
    """
    raise RuntimeError(
        "Database connection details are not included in this repository."
    )